#include<bits/stdc++.h>
#define per(i,a,b) for(int i = (a);i <= (b);++i)
#define rep(i,a,b) for(int i = (a);i >= (b);--i)
using namespace std;
const int maxn = 1000;
int a[maxn],b[maxn];
int c[maxn];
int n = 0;
char s1[maxn],s2[maxn];

int main(){
	scanf("%s",s1);
	scanf("%s",s2);
	printf("-1\n");
	return 0;
}